name: build
description: builds the package
inputs:
  srcDir:
    dir:
      default: .
outputs:
  srcDir:
    dir: {}
run:
  serial:
    - op:
        pkg: { ref: test }
        inputs: { srcDir }
    - op:
        pkg: { ref: github.com/opspec-pkgs/_.pkg.generate-readme#1.0.2 }
        inputs:
          srcDir:
        outputs:
          srcDir: